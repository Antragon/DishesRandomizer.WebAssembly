@page "/Meals"
<RadzenCard class="dialog">
    <h2>Meals</h2>
    <IconButton Class="dialog-card__button" ImageName="plus.png" Click="AddMeal" />
    <div class="dialog__container">
        @foreach (var meal in _mealsSelection) {
            <div class="dialog-card">
                <RadzenTextBox class="dialog-card__value"
                               @ref="TextBoxRef"
                               Value="@meal.Name"
                               Placeholder="<Meal>"
                               Change="name => MealChanged(meal.Id, name)" />
                <IconButton Class="dialog-card__button" ImageName="minus.png" Click="() => DeleteMeal(meal)" />
            </div>
        }
    </div>
    <RadzenPager class="dialog__pager"
                 @ref="_pager"
                 Count="_count"
                 PageSize="_pageSize"
                 PageNumbersCount="5"
                 PageChanged="PageChanged" />
</RadzenCard>